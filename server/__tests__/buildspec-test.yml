# testspec.yml
version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm ci
      - npm install mocha supertest --save-dev
  build:
    commands:
      - npm test
